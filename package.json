{
  "name": "@your-org/skills-installer",
  "version": "1.0.0",
  "description": "CLI tool to install agent skills from the Skills Registry",
  "type": "module",
  "bin": {
    "install-skill": "./scripts/install-skill.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build:registry": "node scripts/build-registry.mjs"
  },
  "keywords": ["skills", "ai", "agents", "claude", "codex"],
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fast-glob": "^3.3.3",
    "yaml": "^2.7.0"
  }
}

