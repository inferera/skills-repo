# 贡献指南

推荐贡献方式（优先）：

1. 将你的 skill 放到公开 GitHub 仓库中（目录内包含 `SKILL.md`）。
2. 使用注册表站点的 `/import` 页面生成导入 issue。
3. 维护者给 issue 加上 `import-approved` 标签后会自动触发 PR。

从这里开始：

- `docs/contributing.zh-CN.md`
- `docs/importer.zh-CN.md`

English version:

- `CONTRIBUTING.md`

